#include <stdio.h>
//定义main函数
int main()
{
    //请在此添加‘求平均数’的代码
    /*****************Begin******************/
    float a, b, c, d, e;
    scanf_s("%f,%f,%f,%f,%f", &a, &b, &c, &d, &e);
    double x;
    x = (a + b + c + d + e) / 5;
    printf("%.2f", x);
    /***************** End ******************/
    return 0;
}