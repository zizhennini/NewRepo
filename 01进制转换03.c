#include <stdio.h>
//定义main函数
int main()
{
    //请在此添加‘进制转换’的代码
    /*****************Begin******************/
    int x;
    scanf_s("%d", &x);
    printf("%o %d %x", x, x, x);   //八进制，十进制，十六进制
    /***************** End ******************/
    return 0;
}