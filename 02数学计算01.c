//包含标准输入输出函数
#include <stdio.h>
//包含数学函数
#include <math.h>
//定义main函数
int main()
{
    //请在此添加‘求指定表达式的值’的代码
    /*****************Begin******************/
    double pi = 3.1415926;
    double a = sin(pi / 4);
    double m = -5 + a;
    double x;
    x = (sqrt(5) + sqrt(3)) / fabs(m);
    printf("%lf", x);
    /***************** End ******************/
    return 0;
}
